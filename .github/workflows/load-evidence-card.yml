name: Load evidence card assets

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Insert evidence card assets
        run: |
          python3 <<'PY'
          from pathlib import Path
          p = Path('src/pages/index.astro')
          s = p.read_text(encoding='utf-8')
          if '/evidence-card.css' not in s:
              s = s.replace('</head>', '    <link rel="stylesheet" href="/evidence-card.css" />\n  </head>')
          if '/evidence-card.js' not in s:
              s = s.replace('</body>', '    <script src="/evidence-card.js" defer></script>\n  </body>')
          p.write_text(s, encoding='utf-8')
          PY
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/pages/index.astro
            git commit -m "refactor: load evidence status card assets"
            git push
          fi
